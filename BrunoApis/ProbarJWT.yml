info:
  name: ProbarJWT
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/hola
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
