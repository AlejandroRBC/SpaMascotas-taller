info:
  name: Registro-Usuario
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/auth/registro
  body:
    type: json
    data: |-
      {
        "email": "admin@spa.com",
        "contrasenia": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |
  Este es un endpoint para mandar el email y contraseña de un usuario
